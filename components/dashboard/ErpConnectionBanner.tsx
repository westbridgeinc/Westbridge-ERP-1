"use client";

/**
 * Previously showed an ERPNext connection warning banner.
 * Removed — customers should not see internal infrastructure details.
 * Kept as a no-op export so existing imports don't break.
 */
export function ErpConnectionBanner() {
  return null;
}
